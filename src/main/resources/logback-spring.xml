<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Propiedades: carpeta de logs y nombre de fichero -->
    <property name="LOG_DIR" value="logs" />
    <property name="LOG_NAME" value="relab" />

    <!-- Console appender -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <!--
        Cambio realizado: se reemplazó <layout> por <encoder> en el ConsoleAppender.
        Razón: Logback moderno ya no admite <layout> como subcomponente, y usar <encoder>
        evita warnings de compatibilidad al arrancar la aplicación.
        Funcionalidad: el patrón de log sigue igual (fecha, nivel, hilo, clase y mensaje),
        con colores y formato, tanto en Windows como en Linux.
        -->
        <encoder>
            <pattern>
                %white(%d{ISO8601}) %highlight(%-5level) [%blue(%t)] %-60.60yellow(%C{20}): %msg%n%throwable
            </pattern>
        </encoder>
    </appender>

    <!-- Rolling File appender -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_NAME}.log</file>
        <encoder>
            <pattern>%d %p %C{1} [%t] %m%n</pattern>
        </encoder>

        <!-- Política de rotado diario y por tamaño -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${LOG_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory> <!-- Guarda solo los últimos 30 días de logs -->
        </rollingPolicy>
    </appender>

    <!-- Nivel de log root -->
    <root level="info">
        <appender-ref ref="RollingFile" />
        <appender-ref ref="Console" />
    </root>
</configuration>
